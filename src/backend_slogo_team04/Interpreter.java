package backend_slogo_team04;

public class Interpreter {
    
    /**
     * This method will iterate through the nonlinear commands and facilitate execution of their commands by providing an object to
     * hold comon state
     * @param headNode The node from which we are to start emulating the commands input
     */
    public void interpretCommandTree(INonLinearCommand headNode){
        
        return;
    }
}
